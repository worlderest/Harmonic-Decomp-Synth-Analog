# 方波的合成与分解
--------------------

模拟电路实验最终课程设计，当时做的一塌糊涂，现在又重新仿真做了一下，发现之前自己确实好菜。

## 甲、总览

实验要求使用运算放大器、电阻、电容构建一套这样的系统：产生特定频率的方波，将方波分解为基波、三次谐波、五次谐波等，最后把所分解得到的谐波合成为方波。

原理是这样的：

根据傅里叶变换可知，方波信号由无数个正弦波组合而成，即：
$$
V(t)=sin(wt)+\frac{1}{3}sin(3wt)+\frac{1}{5}sin(5wt)+...
$$

本实验仅分解基波、三次、五次谐波，更高次的仅仅是重复性动作，况且耗费的运放会大大增多。

电路将会包含：

1. 方波发生器
2. 滤波器
3. 移相器
4. 相加电路  

## 乙、方波发生器

我们决定制作2kHz的方波，首先看一下方波生成电路。

我也不知道这个电路是否足够好，由我原本的实验电路改进而来*（我原本的那个方波发生器并不能很好地工作）*。

![](.\picture\pulse_gen.png)





如图所示，后来我发现这个图似乎并不正确，原因在于Dz这个稳压管没有加分压电阻，因此可能会有大电流，从而导致其不在工作区。

但是原理是通用的，在$V_{out}$从$+V_z$跳到$-V_z$时，同相输入端获得$-\frac{V_z}{2}$的电压,而反相输入端由于电容的存在,会慢慢从$+V_z$到$-\frac{V_z}{2}$,总共需要
$$
T_2=\tau \ln{(\frac{V_{final}-V_{initial}}{V_{final}+V_{initial}})}=\tau \ln3
$$
$T_1$同样如此,因此输出方波周期为
$$
T=2R_p C \ln3
$$
我决定制作2kHz的方波,选择参数以及电路原理图如下:

![ulse_gen](.\picture\pulse_gen1.png)

主要改动部分是增加了一个分压电阻R4,同时后端增加一个电压跟随器用于隔离后面的滤波器.同时运放我选择的是LT1056,这个压摆率是足够的,之前实验用的OP07压摆率低的感人,生成的方波很斜.

其中VCC/VEE为+12V/-12V.计算出频率为2068.72Hz,非常接近2kHz了.输出波形仿真如下:

![ulse](.\picture\pulse1.png)

## 丙、滤波器

### 1) 2kHz带通滤波器

2kHz属于基波，且幅值最大，因此特别容易就可以滤出来。

我选择的滤波器放大倍数为0dB，也就是不放大2kHz的信号，而缩小其他频率信号。

![](.\picture\2k.png)

虽然一级就可以出现较好的波形，但是我还是选择了级联两级，因为这样出来的波形十分完美。

如何选择参数？首先应先根据电路拓扑写出传输方程，从而确定Q(品质因数，决定带宽)、f0(中心频率)、A(中心频率对应的放大倍数)的表达式，它们都是用每个滤波器的两个电容、三个电阻决定。首先我们已经决定要让A为1，f0为2kHz，而Q我们设为3(这似乎是随意的，Q是3这决定了带宽不窄，也就是所容许的中心频率误差大)。并且我们令两个电容相同，这简化了传输方程。现在可以列出三个方程，然而有四个未知数(三个电阻，两个相同的电容)，这要求我们还需要设置一个确定的参数。我们决定设置电容值都为10nF(这似乎是随意的)，由此我们可以解出三个电阻的值。

波形如下：![k_](.\picture\2k_f.png)

级联多次的缺点除了消耗元器件增多，还有初始震荡时间也延长，也就是需要等更长的时间*(约1ms)*来达到最终的波形。

### 2) 6kHz带通滤波器

6kHz相比2kHz的值更小，为1/3，因此滤波更难，至少需要两级才能出现较好的波形。

![](.\picture\6k.png)

以上仅显示了一级的滤波器，参数选择仍然与2kHz滤波器步骤相同。不过稍有不同的是中心交点接地的电阻需要较为精准操控，即使用可变电阻/电位器。这是因为我们的方波并不是标准的方波，应该用FFT分析查看三次谐波真正的频率，应该与6kHz接近但不相同，但是我们需要使我们的滤波器中心频率与其尽量接近。当然这并不是要求我们列参数方程时就使用真正的三次谐波频率，我们只需微调接地电阻即可，将计算出来的电阻分解为一个定值电阻加上一个可变电阻，不断移动可变电阻，并且观察此时电路的频率响应，在精确的三次谐波频率对应的幅值响应为最高时对应的阻值即为所求。

一级是不行的，毕竟基波成分太大了，是三次波的三倍。我们级联三级即可出现较为完好的波形(两级实际也可以接受)。

波形如下：

![k_](.\picture\6k_f.png)

### 3) 10kHz带通滤波器

10kHz幅值更小，为基波的1/5，因此甚至需要级联三次以上。

选择参数、调节可变电阻的步骤不再赘述。

![0](.\picture\10k.png)

仿真显示级联4级出现的波形令我满意。因此我级联了4次。

波形如下：![0k_](.\picture\10k_f.png)

> 这甚至比6kHz的更好看，6kHz放大观看仍能看见起伏不平。

## 丁、移相器

滤波器会带来相位的偏移，而我们合成需要的是同时的相位，即三个正弦波形同时开始从0往上升。

首先我们可以预先估计一下我们所需移动的相位。注意移相需要操作的是基波以外的波，基波是作为相位基准的。

通过FFT分析我发现各个波形的大概相位(忽略幅值)：
$$
y_{2k}=\sin (\omega t + 7 °)\\
y_{6k}=\sin (3\omega t - 187 °)\\
y_{10k}=\sin (5\omega t - 4 °)
$$
**同时从0往上升**：即在同一个$\omega t$三个正弦波相位为0.即$\omega t$为-7°时相位为0，而6k谐波此时相位为-208°即+152°，因此我们需要给它加上-152°的相位偏置。同理10k谐波需要加上+39°的相位偏置。

### 1) 6kHz移相器

电路原理图如下：

![](.\picture\6k_pha.png)

我们可以试图写出其传输方程，这里略过不写。需要注意的是反相输入端所接的反向单位增益放大器电阻只需相同即可，真正需要调整的是同相输入端所接的RC组合。

我们之前所计算出的相位偏置只给了我们大概的阻值，依旧使R为可变电阻不断调整阻值，示波器同时查看2k基波与此时6k移相后的波形，使其同时从0上升。

### 2) 10kHz移相器

与6kHz参数选择同理，需要变化的是10kHz谐波的相位偏置是正的，因此交换RC位置，其作用仅仅是改变传输函数从而改变对相位的影响，这里略过其相位响应。

电路原理图如下：

![](.\picture\10k_pha.png)

## 戊、加法器

我们设计的滤波器使得我们不需要调整各谐波的幅值直接相加即可*(因为我们几乎没有改变它们的幅值，它们仍呈正确的比例)*。

![](.\picture\adder.png)

输出波形如下：![](.\picture\Vout.png)

> 这是符合我的预期的。

## 己、总结

总览整个设计，我发现我用了共14个运算放大器LT1056，若干不符合E24的电阻阻值，这在实践中需要使用电位器实现，然而，我记得当时实验要求运放不超过7个，我仍然没能完成这个指标。*不过所幸最终方波很好看就是了*。